#include <stdio.h>

int main()
{
    int a = 21;
    int b = 2;
    int somma = a + b;
int divisione = a / b ;
int percentuale = a % b;


    printf("la divisione di 21 e 2 è : %d\n " , divisione );
    printf (" la percentuale di 21 e 2 è : %d\n", percentuale);
}